# ADR-003: Local-first cloud adapters

Status: accepted. Deterministic models, local RAG, SQLite, PostgreSQL, Redis and
local MCP transports allow the platform to be built and evaluated without cloud
cost. Interfaces isolate future Azure adapters. Cloud provisioning always
requires explicit human authorization.
